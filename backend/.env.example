# App environment: dev | prod
APP_ENV=dev

FRONTEND_URL=http://localhost:3000
ANTHROPIC_API_KEY=
VOYAGE_API_KEY=

# Chroma local (used when APP_ENV=dev)
CHROMA_HOST=chromadb
CHROMA_PORT=8000

# Chroma Cloud (used when APP_ENV=prod)
# CHROMA_CLOUD_API_KEY=
# CHROMA_CLOUD_TENANT=
# CHROMA_CLOUD_DATABASE=